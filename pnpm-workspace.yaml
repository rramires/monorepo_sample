packages:
  - api
  - web
  - packages/*

# Consolidated from the former per-app pnpm-workspace.yaml files.
# Build scripts allowed to run on install (prisma/esbuild for api, msw for web).
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  prisma: true
  msw: true
